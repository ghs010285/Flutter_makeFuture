import 'package:flutter/material.dart';

class CoupleCodeView extends StatelessWidget {
  const CoupleCodeView({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Text("as"),
    );
  }
}